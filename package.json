{
  "name": "notch-homepage",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix recreate_design",
    "build": "npm run build --prefix recreate_design",
    "install:app": "npm install --prefix recreate_design"
  }
}
